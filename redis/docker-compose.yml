services:
  redis:
    image: registry.cn-beijing.aliyuncs.com/sz-dev/redis
    container_name: redis-server
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - ./data:/data
    command: [ "redis-server", "--requirepass", "sz@123456" ]
    networks:
      - sz-network
networks:
  sz-network:
    external: true
